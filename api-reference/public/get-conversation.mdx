---
title: 'Get Conversation by ID'
api: 'GET /v1/public/conversations/{conversation_id}'
description: 'Retrieve detailed information about a specific conversation using its unique ID'
---

## Overview

This endpoint retrieves comprehensive details about a conversation, including status, transcript, analysis, and metadata. Use this to fetch conversation data after a call has been completed or while it's in progress.

## Authentication

<ParamField header="X-API-Key" type="string" required>
  Your eigi.ai API key. Must be prefixed with `vk_`.
</ParamField>

## Path Parameters

<ParamField path="conversation_id" type="string" required>
  The unique identifier (UUID format) of the conversation to retrieve.

  Example: `35d33a43-176c-405c-92bf-db972035df28`
</ParamField>

## Response

<ResponseField name="conversation_id" type="string">
  Unique identifier for the conversation
</ResponseField>

<ResponseField name="agent_id" type="string">
  ID of the agent that handled this conversation
</ResponseField>

<ResponseField name="conversation_type" type="string">
  Type of conversation: `DAILY`, `TELEPHONY`, `WHATSAPP`, or `CHAT`
</ResponseField>

<ResponseField name="conversation_status" type="string">
  Current status: `IN_PROGRESS`, `COMPLETED`, `DISCONNECTED`, or `FAILED`
</ResponseField>

<ResponseField name="calling_type" type="string">
  Direction of the call: `INBOUND` or `OUTBOUND`
</ResponseField>

<ResponseField name="mobile_number" type="string">
  Phone number involved in the conversation
</ResponseField>

<ResponseField name="transcript" type="array">
  Array of conversation messages with speaker, text, and timestamps
</ResponseField>

<ResponseField name="analysis" type="object">
  AI-generated analysis of the conversation including sentiment, summary, and key points
</ResponseField>

<ResponseField name="duration" type="number">
  Total duration of the conversation in seconds
</ResponseField>

<ResponseField name="created_at" type="datetime">
  Timestamp when the conversation was created
</ResponseField>

<ResponseField name="updated_at" type="datetime">
  Timestamp of the last update to the conversation
</ResponseField>

<ResponseField name="metadata" type="object">
  Custom metadata associated with the conversation
</ResponseField>

## Example Request

<CodeGroup>

```bash cURL
curl -X GET "https://prod.eigi.ai/v1/public/conversations/35d33a43-176c-405c-92bf-db972035df28" \
  -H "X-API-Key: vk_your_api_key_here"
```

```python Python
import requests

conversation_id = "35d33a43-176c-405c-92bf-db972035df28"
headers = {
    "X-API-Key": "vk_your_api_key_here"
}

response = requests.get(
    f"https://prod.eigi.ai/v1/public/conversations/{conversation_id}",
    headers=headers
)

conversation = response.json()
print(conversation)
```

```javascript Node.js
const axios = require('axios');

const conversationId = '35d33a43-176c-405c-92bf-db972035df28';

const response = await axios.get(
  `https://prod.eigi.ai/v1/public/conversations/${conversationId}`,
  {
    headers: {
      'X-API-Key': 'vk_your_api_key_here'
    }
  }
);

console.log(response.data);
```

</CodeGroup>

## Example Response

```json
{
  "conversation_id": "35d33a43-176c-405c-92bf-db972035df28",
  "agent_id": "68ea2517dbb84c09bae1ba0a",
  "conversation_type": "TELEPHONY",
  "conversation_status": "COMPLETED",
  "calling_type": "OUTBOUND",
  "mobile_number": "+919876543210",
  "transcript": [
    {
      "speaker": "agent",
      "text": "Hello! This is Emma from eigi.ai. How can I help you today?",
      "timestamp": "2025-12-04T10:30:15Z"
    },
    {
      "speaker": "user",
      "text": "Hi, I'd like to schedule an appointment.",
      "timestamp": "2025-12-04T10:30:22Z"
    },
    {
      "speaker": "agent",
      "text": "I'd be happy to help you schedule an appointment. What date works best for you?",
      "timestamp": "2025-12-04T10:30:25Z"
    }
  ],
  "analysis": {
    "sentiment": "positive",
    "summary": "Customer requested appointment scheduling. Positive interaction with successful booking.",
    "key_points": [
      "Appointment scheduling request",
      "Customer engagement was positive",
      "Successfully captured customer preferences"
    ],
    "outcome": "success"
  },
  "duration": 245.8,
  "created_at": "2025-12-04T10:30:00Z",
  "updated_at": "2025-12-04T10:34:05Z",
  "metadata": {
    "customer_name": "John Doe",
    "campaign": "Q4_Outreach",
    "priority": "high"
  }
}
```

## Error Responses

<ResponseExample>

```json 401 Unauthorized
{
  "detail": "Invalid API key"
}
```

```json 403 Forbidden
{
  "detail": "API key owner doesn't have access to this conversation"
}
```

```json 404 Not Found
{
  "detail": "Conversation not found"
}
```

```json 500 Internal Server Error
{
  "detail": "Internal Server Error: [error details]"
}
```

</ResponseExample>

## Status Codes

| Status Code | Description |
|-------------|-------------|
| 200 | Successfully retrieved conversation |
| 401 | Missing, invalid, inactive, or expired API key |
| 403 | API key owner doesn't have access to this conversation |
| 404 | Conversation not found |
| 500 | Internal server error |

## Use Cases

<AccordionGroup>
  <Accordion title="Retrieve Call Results">
    After initiating an outbound call, use this endpoint to fetch the complete transcript, analysis, and outcome of the conversation.
  </Accordion>

  <Accordion title="Monitor Active Calls">
    Check the status and real-time transcript of ongoing conversations to monitor agent performance.
  </Accordion>

  <Accordion title="Extract Conversation Data">
    Pull conversation data for analytics, reporting, or integration with CRM systems.
  </Accordion>

  <Accordion title="Quality Assurance">
    Review conversation transcripts and analysis for quality assurance and agent training purposes.
  </Accordion>
</AccordionGroup>

## Notes

- Only conversations associated with agents owned by the API key holder can be accessed
- Conversation data is retained for 90 days by default (configurable in settings)
- Real-time transcripts are available for ongoing conversations
- Analysis data is generated automatically after conversation completion
